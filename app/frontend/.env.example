# Base URL of the backend API (see app/backend)
VITE_API_BASE_URL=http://localhost:4000

# Firebase client config — Auth only (used on the /admin login page to get an
# ID token). Same public values already published in the live site's
# firebase-config.js; safe to publish since access control lives in
# firestore.rules / the backend, not this key.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=vihakids-5db91.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=vihakids-5db91
